[build]
builder = "NIXPACKS"
watchPaths = ["backend/**"]

[deploy]
startCommand = "cd backend && npm start"

[build.nixpacks]
plan = '{ "providers": ["node"], "phases": { "install": { "cmds": ["cd backend && npm install"] } } }'